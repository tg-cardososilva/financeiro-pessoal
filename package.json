{
  "name": "jarvis-financeiro-pessoal",
  "private": true,
  "version": "1.0.0-rc.2",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "test:syntax": "node --check app.js && node --check pwa.js && node --check sw.js",
    "test:rc": "npm run test:syntax && npm test"
  }
}
