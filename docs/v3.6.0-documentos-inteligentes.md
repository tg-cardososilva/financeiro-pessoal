# v3.6.0 — Documentos Inteligentes

## Fronteiras de arquitetura

- Google Drive continua sendo a fonte real do arquivo.
- `jarvis_files` continua sendo o índice canônico de localização, MIME, link, data e contexto/projeto.
- `jarvis_document_processing` contém somente o resultado de processamento ligado por `jarvis_file_id`.
- Nenhum binário é persistido no Supabase Storage ou no Google Cloud Storage.
- Abrir Home, Arquivos, Projetos ou Jarvis não dispara OCR. Processamento e reprocessamento exigem ação explícita.

## Acesso ao Drive

A indexação continua usando `drive.metadata.readonly`. A leitura de conteúdo usa `drive.file`, nunca `drive.readonly`.
O Google Picker recebe um access token `drive.file` efêmero somente em memória. O token do Picker não é persistido, enviado em URL, salvo no banco nem encaminhado ao backend.
Depois da seleção explícita, a autorização por arquivo permite ao backend baixar temporariamente o blob pelo Drive API.

## Pipeline

1. Usuário escolhe `Ler documento`.
2. Google Picker autoriza/seleciona explicitamente PDF, JPG ou PNG.
3. `jarvis-document-processing` valida ownership e metadados do arquivo.
4. O backend baixa os bytes em memória, limite de 20 MB.
5. Bytes são enviados por HTTPS ao worker Cloud Run com assinatura HMAC e expiração curta.
6. O worker valida cabeçalhos assinados antes de ler o corpo, usa a service account `jarvis-document-ai-runtime` sem chave estática e chama o Enterprise Document OCR em `us` por `rawDocument`.
7. Somente o texto OCR retorna ao Supabase Edge Function.
8. OpenAI Responses API, com `store:false` e Structured Outputs, classifica e extrai campos das três famílias suportadas.
9. Persistem somente texto, dados estruturados, status, confiança, erros e telemetria técnica. Os bytes saem de escopo ao fim da requisição.

## Famílias suportadas

### Financeiro
`financial_invoice`, `financial_receipt`, `financial_proof`: estabelecimento, data, total, moeda, itens e forma de pagamento quando detectáveis.

### Contrato / administrativo
`contract`, `administrative`: título/assunto, partes, datas, valores, vencimento e obrigações/cláusulas-chave em resumo.

### Viagem
`travel_reservation`, `travel_ticket`, `travel_lodging`, `travel_other`: companhia/hotel, origem, destino, datas, localizador, passageiros/hóspedes e valores.

## Ações derivadas

A interpretação pode produzir sugestões de evento, tarefa ou conciliação financeira, mas a v3.6.0 não grava automaticamente em Calendar, Tarefas, Finanças ou Projetos.

## Cloud Run e Document AI

- Project: `xenon-lyceum-507716-f3`
- Document AI processor: `e5f4924d6e461967`
- Document AI location: `us`
- Runtime service account: `jarvis-document-ai-runtime@xenon-lyceum-507716-f3.iam.gserviceaccount.com`
- Papel customizado: somente `documentai.processors.get` e `documentai.processors.processOnline`.
- Cloud Run: `jarvis-document-ai-worker`, `southamerica-east1`, escala mínima 0 e endpoint público protegido por HMAC.

A organização bloqueia chaves estáticas de service account (`iam.disableServiceAccountKeyCreation`), por isso o worker usa identidade nativa keyless do Cloud Run.

## Custos

O checkpoint inicial `processors.get` e os testes HTTP 401 do worker não processaram páginas e não geraram consumo de OCR. A aplicação registra por processamento `page_count`, tokens da interpretação e uma referência de preço de tabela do Document AI.

Na tabela pública do Google Cloud consultada em 2026-09-07, Enterprise Document OCR tem faixa gratuita de 0 a 1.000 páginas/counts e, acima dela até 5 milhões, preço de referência de US$ 1,50 por 1.000 páginas. O campo interno de preço por página (`US$ 0,0015`) é uma referência bruta da faixa paga e não substitui o Billing nem aplica automaticamente a franquia gratuita ou créditos promocionais.

O crédito promocional informado no início da v3.6.0 era R$ 1.761,10, com 100% disponível. O consumo real deve ser conferido no Billing do Google Cloud depois dos primeiros documentos processados.

## Fora de escopo

Embeddings, busca vetorial, indexação semântica global, processamento automático do Drive, processamento silencioso em lote, upload pelo Jarvis e automações derivadas sem confirmação.
